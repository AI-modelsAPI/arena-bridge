{$BRIDGE_HOST} {
    encode gzip
    request_body {
        max_size 2GB
    }
    reverse_proxy bridge:3777 {
        # long-running /exec calls: no proxy timeout (Caddy default is none; make it explicit)
        transport http {
            response_header_timeout 0
        }
    }
}
